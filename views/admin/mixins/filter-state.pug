mixin filter-state(listState)
    each item in listState
        button(
            class=`btn btn-sm ms-1 btn-outline-success ${item.class}`
            button-status=item.status
        ) #{item.name}